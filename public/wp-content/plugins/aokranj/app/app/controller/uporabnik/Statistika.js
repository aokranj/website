Ext.define('AO.controller.uporabnik.Statistika', {
    extend: 'Ext.app.Controller',
    
    views: [
        'uporabnik.Statistika'
    ],
    
    refs: [
    ],
    
    init: function() {
        /*
        this.control({
            'ao-main': {
                afterrender: this.onAfterRender
            }
        });
        */
    }
    
});